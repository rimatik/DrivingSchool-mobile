<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="android:Theme.Holo.Light.DarkActionBar">
        <!-- Customize your theme here. -->
    </style>
    <style name="AutoskolaBlue" parent="android:Theme.Light">
        <item name="android:windowBackground">@drawable/background_autoskola_p1</item>
        <item name="android:textColor">@color/white</item>
    </style>
    <style name="AutoskolaBlack" parent="android:Theme.Light">
        <item name="android:windowBackground">@drawable/background_autoskola_black</item>
        <item name="android:textColor">@color/white</item>
    </style>

</resources>
